import React, { useState } from 'react';
import {useSelector} from 'react-redux';



function RoutesListFilterForm() {

    const handleFilterRoutes = () => {

    }

  return (
    <div>
    <Button onClick={handleFilterRoutes}>Filter Routes</Button>
    </div>
  );
}

export default RoutesListFilterForm;